[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "rydberggpt"
version = "0.1.0"
description = "RydbergGPT model"

dependencies = [
    "torch>=2.6.0",
    "numpy"
]
